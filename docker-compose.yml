version: '3.9'

services:
  db:
    image: postgres:13  # Usando PostgreSQL
    volumes:
      - ./db_data:/var/lib/postgresql/data  # Persistência de dados
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 0iCvDs3KpSqQiOLU
      POSTGRES_DB: postgres
    networks:
      - app-network

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
